<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.opro.concurrency.mapper.MemberMapper">

    <insert id="insert" parameterType="com.opro.concurrency.entity.Member"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO member (email, password, nickname, created_at, updated_at)
        VALUES (#{email}, #{password}, #{nickname}, NOW(), NOW())
    </insert>

    <select id="findByEmail" resultType="com.opro.concurrency.entity.Member">
        SELECT id, email, password, nickname, created_at, updated_at
        FROM member
        WHERE email = #{email}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM member
        WHERE email = #{email}
    </select>

    <select id="findAll" resultType="com.opro.concurrency.entity.Member">
        SELECT id, email, password, nickname, created_at, updated_at
        FROM member
        ORDER BY id
    </select>

    <select id="findById" resultType="com.opro.concurrency.entity.Member">
        SELECT id, email, password, nickname, created_at, updated_at
        FROM member
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="com.opro.concurrency.entity.Member">
        UPDATE member
        SET nickname = #{nickname}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM member
        WHERE id = #{id}
    </delete>

</mapper>
